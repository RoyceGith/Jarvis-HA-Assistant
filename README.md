Jarvis Workshop Assistant v0.11.15 — enables operator-configured GitHub Device Flow OAuth for the official GitHub MCP plugin. When github_oauth_client_id is set to a GitHub OAuth App or GitHub App with Device Flow enabled, the Plugin Manager shows a Connect GitHub button that opens GitHub authorization, polls for approval, stores the returned token server-side, and installs the plugin disabled by default.
