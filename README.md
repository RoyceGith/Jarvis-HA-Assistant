ZBRANO v0.12.64 fixes the Workshop Memory approval continuation so approved or denied writes return a correct result instead of failing with `name 'message' is not defined`. The approval reply is now passed explicitly through both streaming and non-streaming chat paths, while follow-up tool rounds, task-scoped Workshop Memory approval, Gmail draft approval isolation, compact plugin controls, OAuth details, and prebuilt ARM64 images remain intact.
