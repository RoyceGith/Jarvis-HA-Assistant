Jarvis Workshop Assistant v0.11.20 — fixes GitHub remote MCP connectivity by using Streamable HTTP/SSE-compatible Accept negotiation, parsing SSE JSON events, normalizing GitHub MCP URL matching, and installing the authorized GitHub connection against the canonical remote endpoint. It also restores immediate persistent New Chat creation so the new conversation opens and appears in the sidebar, and makes Shared Files upload destination/status explicit with cache-busted refresh after shared uploads.
