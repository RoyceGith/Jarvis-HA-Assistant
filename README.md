Jarvis Workshop Assistant v0.11.25 — fixes the v0.11.24 Home Assistant image-build failure by making the legacy New Chat interceptor cleanup idempotent: if the obsolete capture-phase selector exists it is removed, and if it is already absent the build continues. The v0.11.25 release step then advances the backend/HUD version and runs the existing inline-JavaScript and final New Chat wiring validators against the completed generated frontend.
