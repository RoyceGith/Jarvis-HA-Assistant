Jarvis Workshop Assistant v0.11.9 — fixes live Plugin Catalog search without leaving the Plugins tab, binds catalog category, refresh, and install actions against the final DOM, surfaces installation errors and reloads installed plugins after success, and restores New Chat by creating a fresh session, clearing the conversation, resetting the input, and refreshing the chat list when available.
