ZBRANO v0.12.49 sends Telegram notifications with an explicit plain-text parse mode, preventing generated text and Markdown-sensitive punctuation from causing "Can't parse entities" failures. Other notification channels retain their native formatting behavior. The Home Assistant WebSocket transport, unseen-tab indicators, Notification Watchlist, and selectable Delivery Logs remain enabled.
