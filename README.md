Jarvis Workshop Assistant v0.11.10 — repairs chat lifecycle and deletion so empty duplicate chats are no longer created, updates the MCP Plugin Catalog to the official v0.1 registry schema with cursor pagination and visible registry errors, and replaces the GitHub token prompt with GitHub device authorization using a configured OAuth client ID.
