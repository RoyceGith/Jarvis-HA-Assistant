Jarvis Workshop Assistant v0.11.8 — fixes the remaining Docker build failure by replacing complete v0.11.6 version-update statements instead of partially rewriting their search literals, correctly generates backend, JSON, and HUD version 0.11.8 from the v0.11.5 source, and preserves the corrected catalog verifier boundary, final-body typing listener, Plugin Catalog backend repair, increased response spacing, and 3.5% neural opacity while typing.
