Jarvis Workshop Assistant v0.11.24 — removes the obsolete v0.11.9 capture-phase New Chat interceptor that stopped the sidebar ＋ click before the current createNewChat handler could run. New Chat is now owned only by the direct button listener, preserves the visible temporary “New chat” sidebar draft state from v0.11.23, and adds a final generated-HTML wiring validator that fails the build if #new-chat-button is ever intercepted by a capture-phase delegated click handler or if the legacy #clear-chat control returns.
