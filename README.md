ZBRANO v0.12.15 completes Cloudflare OAuth callbacks through Home Assistant Ingress. When Supervisor rejects the first cross-site callback because its SameSite session cookie is absent, ZBRANO recognizes only the expected same-origin callback 401 and repeats it once as a same-site navigation; OAuth state, PKCE, origin, path, and expiry protections remain enforced.
