ZBRANO v0.12.42 places compact conversation controls inside the active selector frame with a consistent inset, preventing Edit/Delete from overlapping the green border. Conversation names and rename fields use a slightly smaller font. v0.12.41 full-row sizing and earlier layout repairs remain enabled.
