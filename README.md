ZBRANO v0.12.38 introduces a compact phone layout that preserves a usable chat viewport. Mobile navigation stays on one horizontally scrollable row, Conversations starts collapsed on first phone use, composer actions are compact, and detailed voice/model controls remain available in Settings instead of consuming Chat height. Conversation rename fields match Edit/Delete height. Automations now hides the system panel label and isolates Safety & Authority forms from Chat form styling to prevent overlapping controls. v0.12.37 Playwright chat isolation remains enabled.
