Jarvis Workshop Assistant v0.11.22 — repairs New Chat so the sidebar ＋ always resets into a fresh unsaved draft even when a stale request/socket exists; removes the legacy duplicate New Chat control from Entities; and makes Shared Files visibly report loading, empty-library, and API-error states instead of appearing blank. Shared uploads continue to refresh the library immediately, while chat-only uploads remain scoped to their conversation.
