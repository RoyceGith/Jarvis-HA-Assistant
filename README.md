ZBRANO v0.12.35 repairs approved remote-plugin continuations by sending the OpenAI MCP approval response with only `approval_request_id` and `approve: true`. The unsupported `reason` parameter is omitted for approvals, while v0.12.34 local terminal cancellation, native approval enforcement, provider-aware summaries, argument redaction, and activity-row ordering remain enabled.
