ZBRANO v0.12.33 makes remote-plugin approvals provider-aware and cancellation final. Approval prompts identify Cloudflare, GitHub, or the actual installed plugin; show a concise operation such as `GET /zones`; and no longer dump code payloads or unnecessary identifiers into Chat. Cancelling now ends the pending action immediately without asking again, and the correct plugin activity is marked cancelled. v0.12.32 citation curation remains enabled.
