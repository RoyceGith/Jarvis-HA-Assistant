Jarvis Workshop Assistant v0.11.6 — fixes the Plugin Catalog HTTP 500 caused by inconsistent cached-result tuple lengths, validates catalog backend return contracts, improves structured error display, increases spacing between chat response sections, and reduces the neural background to 3.5% opacity while the user is typing.
