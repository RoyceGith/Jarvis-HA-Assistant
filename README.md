Jarvis Workshop Assistant v0.11.7 — repairs the v0.11.6 Docker build verifier so catalog contract validation stops at the correct function boundary, moves the typing-opacity listener into the final application script after the message input exists, and preserves the Plugin Catalog backend fix, increased response spacing, and 3.5% neural opacity while typing.
