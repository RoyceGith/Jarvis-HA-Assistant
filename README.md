ZBRANO v0.12.31 adds live, evidence-based tool activity to Chat. Active plugin icons pulse only while their tool is running, readable phases identify web search, Home Assistant, Workshop Memory, browser inspection, and Developer investigation work, and a compact per-response timeline shows elapsed time, completion, failure, or approval-required state. Attach and attachment-destination controls now match the compact Web selector styling. The timeline uses actual backend tool events and preserves every existing approval and Developer Mode boundary.
