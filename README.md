Jarvis Workshop Assistant v0.12.2 fixes device-file attachments by giving the late attachment controller ownership of selection, upload, pending attachment IDs, and chip rendering. Developer diagnostics now perform and clean up a real temporary text-file upload instead of reporting health from DOM presence alone.
