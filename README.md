ZBRANO v0.12.55 makes the Entities inventory table customizable. Drag any column header to move it, drag the header's right edge to resize it, and use Reset columns to restore the original layout. Column order and widths persist locally on the current browser/device. The table remains horizontally scrollable for narrow screens and keeps entity selection, access policy, aliases, filtering, and live state behavior intact.
