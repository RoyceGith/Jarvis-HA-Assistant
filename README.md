Jarvis Workshop Assistant v0.12.7 shows attached filenames, types, and sizes directly beneath the user message and restores those file references with saved conversations. Attachment status text now names each file instead of reporting only a count, while model-only extracted file context remains separate from the public chat display. v0.12.6 Developer policy, indicator, simplified Chat layout, and softer typography remain intact.
