ZBRANO v0.12.17 fixes the add-on image cleanup failure that prevented the Playwright release from installing. Docker's final temporary-source cleanup is now idempotent, so an already-consumed historical patch cannot fail the otherwise successful image build. The v0.12.16 Playwright MCP browser inspection, local-only restrictions, plugin health card, and diagnostics remain included.
