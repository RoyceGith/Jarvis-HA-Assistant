ZBRANO v0.12.43 adds a Notification Center inside Automations. It discovers Home Assistant notify entities, identifies Telegram channels, stores default delivery and quiet-hour policies, sends explicit test notifications, records bounded delivery outcomes, participates in diagnostics and backup/restore, and keeps BotFather credentials entirely inside Home Assistant. v0.12.42 interface repairs remain enabled.
