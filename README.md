Jarvis Workshop Assistant v0.11.21 — corrects New Chat to use a browser-side unsaved draft: clicking ＋ switches to Chat immediately, creates a fresh draft session ID, clears the transcript/input and pending attachment selection, and does not create a permanent conversation until the first message is sent. Shared Files remain independent and are not deleted when starting a new draft.
