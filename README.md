ZBRANO v0.12.34 requires remote plugin actions to enter the platform-native approval gate directly instead of allowing the AI to compose a manual pre-approval prompt. Raw arguments, executable code, and account identifiers remain hidden, and denial remains terminal. In Chat, the tool activity row now appears directly below Attach and the overall Completed row appears beneath it immediately above the prompt; no activity indicator was removed. v0.12.33 provider-aware approvals remain enabled.
