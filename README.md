Jarvis Workshop Assistant v0.11.16 — restores Home Assistant entity discovery with WebSocket-first inventory and REST fallback, surfaces inventory diagnostics instead of silently showing none, exports the configured GitHub OAuth client ID into the runtime, and renames the assistant chat label to ZBRANO.
