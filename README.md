Jarvis Workshop Assistant v0.11.14 — repairs the deterministic v0.11.13 image-build failure by fixing the install-routing patch's catalog-response source matcher to accept the one-line route generated by the existing patch chain, executes that source repair immediately before v0.11.13 during the Docker build, and preserves the explicit GitHub install routing and popup guard introduced in v0.11.13.
