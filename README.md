Jarvis Workshop Assistant v0.11.19 — repairs the v0.11.18 frontend JavaScript parse failure that prevented Plugins, Shared Files, and Entities navigation; removes the global WebSocket override and sends attachment IDs through the existing chat socket; keeps the Attach button on the browser-native current-device file picker for LAN or Tailscale access; and adds build-time inline JavaScript syntax validation.
