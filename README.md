Jarvis Workshop Assistant v0.11.12 — fixes the remaining Plugin Manager JavaScript temporal-dead-zone failure by removing loadPlugins() dependence on the top-level pluginList lexical binding, resolving the live #plugin-list node at call time for loading and error rendering, and binding plugin list click/change actions through the live DOM resolver. This preserves the expanded MCP catalog discovery and chat lifecycle fixes from earlier v0.11.x releases.
