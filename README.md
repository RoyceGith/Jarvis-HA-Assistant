ZBRANO v0.12.58 keeps installed plugin cards compact by hiding tool settings behind the Settings toggle, including high-tool-count plugins like Cloudflare. It preserves the v0.12.57 Gmail Direct connector, which uses the standard Gmail REST API for bounded mail search, label listing, thread reading, and approval-gated unsent draft creation.
