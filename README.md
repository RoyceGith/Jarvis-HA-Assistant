Jarvis Workshop Assistant v0.11.23 — makes New Chat visibly enter a temporary active sidebar state immediately: clicking ＋ creates a fresh unsaved draft, inserts one active “New chat” row at the top of Conversations, clears the previous active highlight, and replaces any unused draft instead of stacking duplicates. Opening a persisted conversation removes the temporary draft row, and after the first message the normal chat-list refresh replaces the draft with the saved conversation.
