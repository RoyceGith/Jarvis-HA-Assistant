Jarvis Workshop Assistant v0.11.13 — fixes Plugin Catalog install routing so only the exact official GitHub entry is intercepted for GitHub OAuth, publishes OAuth availability from the backend, disables GitHub authorization before click when this build has no registered Jarvis GitHub OAuth application, prevents the blank-tab flash/close behavior, and keeps ordinary remote HTTPS plugins on the normal in-page install path while package-only Registry entries remain non-installable.
