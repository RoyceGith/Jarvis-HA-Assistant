ZBRANO v0.12.22 repairs the v0.12.21 image build by making config.yaml available to the release-manifest validator and removing the temporary copy immediately after validation. Automatic, idempotent, backup-backed Workshop Memory release synchronization remains enabled, observable, and controlled from Settings → Updates.
