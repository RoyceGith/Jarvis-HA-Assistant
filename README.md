ZBRANO v0.12.50 routes Telegram channels through the supported `telegram_bot.send_message` action with `plain_text`, preventing both message parsing failures and the generic notify action's unsupported-key error. Non-Telegram channels continue through `notify.send_message`. WebSocket delivery, unseen-tab indicators, Notification Watchlist, and selectable Delivery Logs remain enabled.
