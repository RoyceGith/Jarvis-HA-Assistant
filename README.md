Jarvis Workshop Assistant v0.11.11 — expands MCP Plugin Catalog discovery beyond remote-only entries by keeping package-based Registry servers visible, increases cursor pagination coverage, rejects stale featured-only catalog caches, distinguishes discoverable package entries from directly installable remote HTTPS servers, and removes the misleading end-user GitHub OAuth client-ID configuration. GitHub one-click OAuth remains blocked until Jarvis itself has a centrally registered GitHub OAuth/GitHub App client ID, which GitHub requires for its MCP authorization flow.
