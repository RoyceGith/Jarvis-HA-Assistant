{
  "version": "0.12.22",
  "project": "ZBRANO Workshop Assistant",
  "note": "Release and Change Log.md",
  "source": "RoyceGith/Jarvis-HA-Assistant main",
  "summary": "Repair the release-manifest build gate while retaining automatic Workshop Memory release synchronization.",
  "features": [
    "Automatically record each newly installed ZBRANO version in Workshop Memory.",
    "Keep the standing-authorization toggle, manual retry control, release-sync API, and Developer diagnostic status."
  ],
  "fixes": [
    "Copy config.yaml into the build workspace before release validation to prevent the v0.12.21 FileNotFoundError.",
    "Remove the temporary configuration copy after validation so it is not retained in the runtime image.",
    "Preserve duplicate protection, non-blocking retries, and backup-backed Workshop Memory replacement."
  ],
  "validation": [
    "Complete generated patch chain",
    "Inline JavaScript validation",
    "New Chat wiring validation",
    "Backend Python compilation",
    "Release synchronization regression coverage"
  ]
}
