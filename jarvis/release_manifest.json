{
  "version": "0.12.31",
  "project": "ZBRANO Workshop Assistant",
  "note": "Release and Change Log.md",
  "source": "RoyceGith/Jarvis-HA-Assistant main",
  "summary": "Show live plugin and tool activity with readable phases, elapsed time, results, and approval state.",
  "features": [
    "Pulse the matching enabled-plugin icon only while its MCP tool is actively running.",
    "Show readable phases for web search, Home Assistant, Workshop Memory, Playwright inspection, and Developer investigation.",
    "Add a compact per-response timeline with live elapsed time and completed, failed, or approval-required state.",
    "Match the Attach button and attachment-destination selector to the compact Web control styling.",
    "Keep automatic Workshop Memory release synchronization enabled."
  ],
  "fixes": [
    "Replace opaque long-running tool feedback with real backend lifecycle events.",
    "Stop active indicators reliably when a response completes, fails, is stopped, or the socket closes.",
    "Limit the visual timeline to eight compact entries while preserving the response transcript.",
    "Preserve all approval gates, local-only browser scope, and Developer Mode tool isolation."
  ],
  "validation": [
    "Complete generated patch chain",
    "Inline JavaScript validation",
    "New Chat wiring validation",
    "Backend Python compilation",
    "Streaming activity metadata and lifecycle regression coverage",
    "Plugin identity, approval-state, reduced-motion, and elapsed-time UI coverage",
    "Release and Workshop Memory synchronization metadata validation"
  ]
}
